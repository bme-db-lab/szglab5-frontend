{{#if errorMessage}}
  {{login-box goToSettings=(action 'goToSettings') redirect=redirect error=errorMessage hasError=true}}
{{else}}
  {{login-box goToSettings=(action 'goToSettings') redirect=redirect }}
{{/if}}


{{redirectChanged}}
